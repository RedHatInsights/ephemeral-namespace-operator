package controllers

import (
	"time"

	. "github.com/onsi/ginkgo/v2"
)

var _ = Describe("", func() {
	const (
		timeout  = time.Second * 35
		duration = time.Second * 30
		interval = time.Millisecond * 30
	)

	Context("", func() {

	})
})
